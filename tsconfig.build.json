{
  // Build type-check: `tsc --noEmit -p tsconfig.build.json` verifies src only (tests are
  // excluded). esbuild does the actual emit (see scripts/bundle.mjs), so no tsc output here.
  "extends": "./tsconfig.json",
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules", "dist", "test"]
}
